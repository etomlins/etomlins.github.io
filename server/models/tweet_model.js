class Tweet {
  constructor(username, content, timestamp, userId) {
    this.username = username;
    this.content = content;
    this.timestamp = timestamp;
    this.userId = userId;
  }
}

module.exports = Tweet;
